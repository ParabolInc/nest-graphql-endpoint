import { AliasMapper, EndpointResponseData } from './types';
declare const dealiasResult: (data: EndpointResponseData, aliasMapper: AliasMapper) => EndpointResponseData;
export default dealiasResult;
